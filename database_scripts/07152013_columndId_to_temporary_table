ALTER TABLE `colfusion_temporary` ADD COLUMN `cid` VARCHAR(45) NULL  AFTER `columnnum` ;

update colfusion_temporary t1
set cid = 	(	
				SELECT cid 
				FROM colfusion_dnameinfo
				where sid = t1.sid and dname_chosen = t1.dname 				
			)
where cid is null;

